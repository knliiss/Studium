server:
  port: ${TESTING_PORT:8087}
  shutdown: graceful

spring:
  application:
    name: testing-service

  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:postgres}?currentSchema=${TESTING_DB_SCHEMA:testing}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}

  sql:
    init:
      continue-on-error: true
      mode: always

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 2s
      connect-timeout: 2s

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    show-sql: false

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:29092}
    producer:
      acks: all
      retries: 5
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    listener:
      ack-mode: record
      missing-topics-fatal: false
      observation-enabled: true
    consumer:
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: dev.knalis.contracts.event
    template:
      observation-enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
  metrics:
    tags:
      application: ${spring.application.name}
      instance: ${HOSTNAME:${spring.application.name}}

logging:
  level:
    root: INFO
    org.springframework.security: INFO

app:
  kafka:
    topics:
      test-published: testing.test-published.v1
      test-started: testing.test-started.v1
      test-completed: testing.test-completed.v1
    outbox:
      enabled: ${TESTING_OUTBOX_ENABLED:true}
      batch-size: ${TESTING_OUTBOX_BATCH_SIZE:50}
      publish-interval: ${TESTING_OUTBOX_PUBLISH_INTERVAL:2s}
      processing-timeout: ${TESTING_OUTBOX_PROCESSING_TIMEOUT:30s}
      send-timeout: ${TESTING_OUTBOX_SEND_TIMEOUT:10s}
      max-attempts: ${TESTING_OUTBOX_MAX_ATTEMPTS:10}
      initial-retry-delay: ${TESTING_OUTBOX_INITIAL_RETRY_DELAY:1s}
      max-retry-delay: ${TESTING_OUTBOX_MAX_RETRY_DELAY:5m}
      published-retention: ${TESTING_OUTBOX_PUBLISHED_RETENTION:168h}
      cleanup-interval: ${TESTING_OUTBOX_CLEANUP_INTERVAL:1h}
  testing:
    jwt:
      issuer: ${JWT_ISSUER:dev.knalis.auth-service}
      audience: ${JWT_AUDIENCE:dev.knalis.api}
      public-key-path: ${JWT_PUBLIC_KEY_PATH:file:/opt/keys/public.pem}
    education-service:
      base-url: ${TESTING_EDUCATION_SERVICE_BASE_URL:http://localhost:8085}
      connect-timeout: ${TESTING_EDUCATION_SERVICE_CONNECT_TIMEOUT:2s}
      read-timeout: ${TESTING_EDUCATION_SERVICE_READ_TIMEOUT:5s}
      shared-secret: ${EDUCATION_INTERNAL_SHARED_SECRET}
    notification-service:
      base-url: ${TESTING_NOTIFICATION_SERVICE_BASE_URL:http://localhost:8084}
      connect-timeout: ${TESTING_NOTIFICATION_SERVICE_CONNECT_TIMEOUT:2s}
      read-timeout: ${TESTING_NOTIFICATION_SERVICE_READ_TIMEOUT:5s}
      shared-secret: ${NOTIFICATION_INTERNAL_SHARED_SECRET}
    audit-service:
      base-url: ${TESTING_AUDIT_SERVICE_BASE_URL:http://localhost:8090}
      connect-timeout: ${TESTING_AUDIT_SERVICE_CONNECT_TIMEOUT:2s}
      read-timeout: ${TESTING_AUDIT_SERVICE_READ_TIMEOUT:5s}
      shared-secret: ${AUDIT_INTERNAL_SHARED_SECRET}
    reminders:
      enabled: ${TESTING_REMINDERS_ENABLED:true}
      check-interval: ${TESTING_REMINDERS_CHECK_INTERVAL:30m}
      lookback: ${TESTING_REMINDERS_LOOKBACK:35m}
